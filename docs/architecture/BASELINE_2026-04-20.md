# Baseline (2026-04-20)

This baseline fixes the current state before refactoring.

## Code Size Hotspots (Initial)

- `apps/frontend/webapp/app.js`: 7967 LOC
- `apps/frontend/webapp/index.html`: 1506 LOC
- `apps/frontend/webapp/style.css`: 5620 LOC
- `apps/backend/src/daralla_backend/web/routes/api_user.py`: 915 LOC
- `apps/backend/src/daralla_backend/services/xui_service.py`: 1062 LOC
- `apps/backend/src/daralla_backend/services/subscription_manager.py`: 909 LOC

## Coupling Signals

- Single runtime entrypoint: `python -m daralla_backend` (`apps/backend/src/daralla_backend/__main__.py`)
- Single deployment service: `docker-compose.yml` (`telegram-bot`)
- Shared state DB: `apps/backend/src/daralla_backend/db/__init__.py` (`daralla.db`)
- Direct `daralla_backend.db` imports in routes detected in:
  - `apps/backend/src/daralla_backend/web/routes/api_user.py`
  - `apps/backend/src/daralla_backend/web/routes/payment.py`
  - `apps/backend/src/daralla_backend/web/routes/admin_users.py`
  - `apps/backend/src/daralla_backend/web/routes/admin_stats.py`
  - `apps/backend/src/daralla_backend/web/routes/admin_subscriptions.py`
  - `apps/backend/src/daralla_backend/web/routes/admin_broadcast.py`

## Testing Baseline

- Test files under `tests/`: 16
- Integration checks exist for key routes, but critical flows still need deeper scenario coverage:
  - user registration + trial lifecycle
  - payment webhook idempotency
  - sync error behavior

## Operational Baseline (to fill from production telemetry)

The following SLO indicators are required to be recorded from production logs/monitoring:

- `GET /api/*` p95 latency
- `/webhook/yookassa` and `/webhook/cryptocloud` success rate
- background sync failure rate
- deploy duration and post-deploy incident rate

## Reproducibility

To refresh static architecture counters locally:

```bash
python scripts/collect_baseline_metrics.py
```

## Refresh Snapshot (Post modular-boundary stage)

Source: `python scripts/collect_baseline_metrics.py`

- `apps/frontend/webapp/app.js`: 3155 LOC (from 7967, `-4812`)
- `apps/frontend/webapp/index.html`: 1544 LOC (from 1506, `+38`)
- `apps/frontend/webapp/style.css`: 5620 LOC (no change)
- `apps/backend/src/daralla_backend/web/routes/api_user.py`: 89 LOC (from 915, `-826`)
- `apps/backend/src/daralla_backend/services/xui_service.py`: 931 LOC (from 1062, `-131`)
- `apps/backend/src/daralla_backend/services/subscription_manager.py`: 834 LOC (from 909, `-75`)

Function counts:

- `apps/frontend/webapp/app.js:function`: 81
- `apps/backend/src/daralla_backend/web/routes/api_user.py:def`: 15
- `apps/backend/src/daralla_backend/services/xui_service.py:def`: 37
- `apps/backend/src/daralla_backend/services/subscription_manager.py:def`: 10

Test inventory:

- test files under `tests/`: 17
- full pytest on this stage: `85 passed`

Architecture guardrails status:

- direct `daralla_backend.db*` imports in `apps/backend/src/daralla_backend/web/routes/*`: 0 (enforced by `scripts/check_arch_rules.py`)
- passthrough wrappers in `apps/frontend/webapp/app.js`: 0 (guardrail set to `<= 15`)
- HTTP contract validation: passing (`scripts/check_http_contracts.py`)
- frontend entry smoke: passing (`scripts/check_frontend_smoke.py`)

KPI/DoD snapshot:

- `apps/frontend/webapp/app.js` reduced below `7k` LOC target for this phase (`3155`).
- route-layer remains free of direct `daralla_backend.db` imports (`0` violations).
- contract examples expanded (`24` JSON examples, type checks enabled in contract validator).
- CI-equivalent local gate: `arch + contracts + frontend smoke + pytest` all green.
- architecture refactor status: **closed** (freeze policy enabled; structural changes by ADR only).

Apps-first migration status:

- backend runtime moved to `apps/backend/src/daralla_backend`
- frontend runtime moved to `apps/frontend/webapp`
- runtime/deploy model remains single-service (no microservice split in this stage)
