services:
  telegram-bot:
    build: .
    container_name: daralla-bot
    restart: unless-stopped
    logging:
      driver: json-file
      options:
        max-size: "5m"
        max-file: "2"
    environment:
      # Переменные окружения загружаются из .env файла
      - TELEGRAM_TOKEN=${TELEGRAM_TOKEN}
      - ADMIN_IDS=${ADMIN_ID}
      - YOOKASSA_SHOP_ID=${YOOKASSA_SHOP_ID}
      - YOOKASSA_SECRET_KEY=${YOOKASSA_SECRET_KEY}
      - WEBAPP_URL=${WEBAPP_URL:-}
      - REMNAWAVE_API_URL=${REMNAWAVE_API_URL:-}
      - REMNAWAVE_API_KEY=${REMNAWAVE_API_KEY:-}
      - SUBSCRIPTION_URL=${SUBSCRIPTION_URL:-}
      - REMNAWAVE_SUBSCRIPTION_URL_TEMPLATE=${REMNAWAVE_SUBSCRIPTION_URL_TEMPLATE:-}
      # Порт веб-сервера Quart/Hypercorn (должен совпадать с ports: и nginx)
      - WEBHOOK_PORT=${WEBHOOK_PORT:-5000}
      - WEBHOOK_REPLAY_WINDOW_SECONDS=${WEBHOOK_REPLAY_WINDOW_SECONDS:-300}
      - YOOKASSA_WEBHOOK_SECRET=${YOOKASSA_WEBHOOK_SECRET:-}
      - CRYPTOCLOUD_WEBHOOK_SECRET=${CRYPTOCLOUD_WEBHOOK_SECRET:-}
      - CRYPTOCLOUD_HMAC_SECRET=${CRYPTOCLOUD_HMAC_SECRET:-}
      - REMNAWAVE_WEBHOOK_SECRET_HEADER=${REMNAWAVE_WEBHOOK_SECRET_HEADER:-}
      # Опциональные переменные (могут быть пустыми)
      - NGROK_AUTH_TOKEN=${NGROK_AUTH_TOKEN:-}
      - WEBHOOK_URL=${WEBHOOK_URL:-}
      - WEBSITE_URL=${WEBSITE_URL:-}


    ports:
      # Открываем порт для webhook'ов YooKassa
      - "5000:5000"
    volumes:
      # Монтируем папку для баз данных
      - ./data:/app/data:rw
      # Монтируем .env файл если он есть
      - ./.env:/app/.env:ro
    networks:
      - bot-network
    # Ограничения ресурсов для продакшена
    deploy:
      resources:
        limits:
          memory: 512M
        reservations:
          memory: 256M

networks:
  bot-network:
    driver: bridge
